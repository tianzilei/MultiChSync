# ============================================
# MultiChSync - 多模态神经影像数据转换工具
# 依赖要求文件
# ============================================

# 核心依赖
numpy>=1.21.0
pandas>=1.3.0
h5py>=3.0.0
scipy>=1.7.0
networkx>=3.0

# SNIRF格式支持 (fNIRS转换)
snirf>=0.5.0

# ECG转换依赖
bioread>=2.0.0    # ACQ文件读取
neurokit2>=0.2.0  # ECG处理（提供双后端支持）

# EEG转换依赖
mne>=1.0.0        # EEG文件读取和处理
pybv>=0.5.0       # BrainVision格式导出

# 可选依赖（如需EDF格式输出）
# pyedflib>=0.1.0  # EDF格式导出（仅EEG模块需要）