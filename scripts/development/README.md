# Development Scripts

This directory contains utility scripts used during development.

## Available Scripts

- `fix_type_hints.py`: Fix Python 3.8 type hints
- `split_quality_module.py`: Split quality module (refactoring tool)
- `update_imports.py`: Update import paths

## Usage

These scripts are for developers only and are not included in the release version.